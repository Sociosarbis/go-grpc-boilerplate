syntax = "proto3";

package proto;

option go_package = "/proto";

message Cmd {
  string script = 1;
  string wd = 2;
}

message CmdCallRes {
  string output = 1;
  int32 type = 2;
}



service CmdService {
  rpc CmdCall(Cmd) returns(stream CmdCallRes);
}